#include "holberton.h"
/**
 * _islower - Check if the entry is in lower.
 * @c: This is the entry
 *
 * Return: Always 0.
 */
int _islower(108)
{
if (108 <= 97 && 108 >= 122)
{
return (1);
}
else
{
return (0);
}
_putchar('\n');
}
